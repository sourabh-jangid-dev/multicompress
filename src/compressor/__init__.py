# This empty file tells Python that "compressor" is a PACKAGE
# (a folder you can import from, e.g. `from compressor.image_compressor import compress_image`).
# Every importable Python package needs one of these.
